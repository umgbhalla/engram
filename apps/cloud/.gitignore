node_modules/
.wrangler/
src/modules.gen.js
# Build artifacts regenerated by scripts/build-stdlib.mjs (run via wrangler build.command).
src/stdlib.bundle.txt
src/stdlib-meta.json
node_modules/

# No *.wasm gets silently force-added again (e.g. into src/).
*.wasm

# Deliberately vendored binaries — all cloud vendored bytes live here, tracked on purpose.
!vendor/**

# generated baked Rust-kernel modules
src/modules.rust.gen.js
src/modules.gen.js

# esbuild bundle output (the wrangler entry, regenerated by scripts/build.mjs)
dist/
