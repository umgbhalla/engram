[package]
name = "zstdcodec"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
zstd-safe = { version = "7", default-features = false }

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
strip = true
