[package]
name = "engine"
version = "0.1.0"
edition = "2021"

[dependencies]
rquickjs = { path = "vendor/rquickjs", default-features = false, features = ["std", "bindgen"] }

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
strip = true
