# Packet A: Topic Clone

Objective: enumerate `topic:recursive-language-models` and clone/fetch every repo.

Result: GitHub topic API returned 20 repositories. All 20 exist under
`/tmp/rlm-substrate-research`.

Verification: compared API full names after owner/repo path normalization against
local directories.
