{
  "name": "montydyn-exp4b",
  "private": true,
  "type": "module",
  "//": "quickjs-wasi is intentionally a devDependency, NOT a dependency: worker-build 0.8.3 merges `dependencies` into the wasm-bindgen snippet package.json and then mis-parses it (`invalid type: map, expected a string`). esbuild resolves it from node_modules regardless of which field it sits in. See docs/results/exp-4b.md.",
  "devDependencies": {
    "quickjs-wasi": "3.0.0",
    "ws": "^8.18.0"
  }
}
