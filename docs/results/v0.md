# montydyn v0 — integrated durable REPL kernel — RESULT

> Branch `feat/v0-kernel`. Built on PATH (b) (Rust DO shell + JS glue), integrating
> EXP-4b (Rust DO + quickjs-wasi), EXP-9 (per-cell checkpoint + engine-hash guard),
> EXP-8 (seeded determinism), EXP-6/7 (streaming dump + size ceilings). 2026-06-01.

## Verdict

**PASS.** The full durable-REPL loop works on real Cloudflare, including a **real idle
eviction** (DO generation 1→2): eval → per-cell SQLite checkpoint → kernel gone →
reconnect → eval restores from SQLite and advances, with **no replay** and **no
re-fired side effects**.

Deployed worker: **`montydyn-v0`** → `https://montydyn-v0.umg-bhalla88.workers.dev`
Version ID at this result: `c8eea08a-cbe3-4ee1-ba84-ef684d154391`.

## What changed vs the experiments

| Concern | EXP behaviour | v0 |
|---|---|---|
| Snapshot store | R2 blob (100% network RTT, ~230ms p50) | **DO SQLite** chunked (~64KB rows), R2 only as >2MB-gz overflow |
| Dump | `serializeSnapshot` + 2nd full copy (~3× transient, EXP-6) | **streaming gzip** (serialize → CompressionStream → single Uint8Array) |
| Engine hash | runtime double-import of wasm bytes (EXP-9) | **baked in at build time** (`scripts/engine-hash.mjs` → `globalThis.__ENGINE_HASH`) |
| Determinism | proven locally (EXP-8) | integrated into the live kernel: seeded clock/RNG/crypto + persisted entropy counters (`clockCalls`,`rngCalls`) per snapshot |
| Size guard | probe-only | in-kernel admission control (refuse >~50MB live / >~20MB raw restore) |
| Concurrency | n/a | in-DO async mutex (JS promise-chain); cell# allocated in critical section |
| Protocol | `cell`/`eval`/`crash` | `eval` (auto-checkpoint), `reset` (clear+epoch++), `gen` |

## Smoke evidence (exact values)

### 1. Simulated in-memory drop (`evict`)
```
eval setup: x=42  → checkpoint {cell:0, store:"sqlite", nChunks:2, sizeRaw:1310748, sizeGz:103190, stackPointer:1048576}
evict     : droppedInMemory=true
gen       : inMemory=false, committedCell=0, engineHash=52d4a276…eb24
eval "x"  : value=42,  restoreSource="sqlite-restore"
eval "inc()": value=43, restoreSource="warm"
>>> PASS (simulated): x=42 inc()=43
```

### 2. REAL idle eviction (cold wake, ~75s idle)
```
setup gen : generation=1, inMemory=true
... 75s disconnected idle ...
gen       : generation=2, inMemory=false        ← DO genuinely reconstructed
eval "x"  : value=42,  restoreSource="sqlite-restore", generation=2
eval "inc()": value=43, restoreSource="warm"
>>> PASS (real cold wake): x=42 inc()=43
cold-wake reconstruction: gen 1 -> 2 => RECONSTRUCTED (real eviction)
```

### 3. Reset
```
reset     : epoch 0 -> 1
eval "typeof x": value="undefined", restoreSource="fresh"
>>> PASS reset cleared namespace
```

## Restore latency

`restoreLatencyMs` reported by the DO reads **0** because workerd clamps `Date.now()`
during synchronous compute (Spectre mitigation) — it does not advance across the
restore. So latency was measured **client-side** as the difference between a cold
(restore-triggering) eval round-trip and a warm eval round-trip on the same warm DO:

```
coldRoundTripMs : 155
warmRoundTripMs : 147
restoreCost     : ~8 ms   (SQLite read + gunzip + deserialize + blit)
restoreSource   : sqlite-restore
```

At v0 image sizes (~1.3MB raw / ~103KB gz) the SQLite-local restore cost is **~8ms** —
confirming the EXP-7 finding that decode/instantiate/blit is sub-compute, and the only
reason the R2 path was ~230ms p50 was network RTT. **Moving the hot store to SQLite
removes that RTT.** Total client-observed cold-wake round-trip (~155ms) is dominated by
the client↔edge WebSocket network leg plus DO wake, not by the restore itself.

## Snapshot shape at v0

- Namespace `globalThis.x=42; globalThis.inc=()=>++x` →
  raw image 1,310,748 B, gzip 103,190 B (~12.7× — matches the EXP-4b/7 ~12× ratio).
- Stored as 2 SQLite chunks (`store:"sqlite"`), single `snap_manifest` row.
- Entropy counters persisted: `clockCalls:2, rngCalls:4` (replayed on restore).
- `stackPointer:1048576` captured and restored.

## Files

- `v0/src/lib.rs` — Rust DurableObject shell (SQLite chunked store + R2 overflow,
  mutex, epoch/generation, per-cell checkpoint with commit ordering).
- `v0/src/glue.js` — JS kernel (seeded determinism, streaming dump, engine-hash +
  size guards, restore).
- `v0/entry.mjs` — CompiledWasm + engine-hash bake-in wrapper.
- `v0/test-client.mjs` — smoke test.

## Open / follow-up

- **Latency timing inside the DO is unobservable** via `Date.now()` (workerd clamp).
  For real telemetry, measure client-side or thread timings through an external clock;
  do not trust in-isolate sub-call deltas.
- **Streaming gunzip into WASM memory** (raise the >30MB raw restore ceiling) — not
  needed at v0 sizes; deferred (per SUMMARY).
- **R2 overflow path is implemented but unexercised** at v0 sizes (gz < 2MB always
  takes the SQLite path). Validate the overflow branch with a large namespace before
  relying on it.
- **`snapshot` replace is delete-then-insert** within one synchronous handler (DO is
  single-threaded, serialized by the mutex) — adequate, but not a SQL transaction; a
  crash mid-replace between DELETE and INSERT would lose the snapshot. Wrapping the
  checkpoint write in a SQLite transaction is a cheap hardening follow-up.
