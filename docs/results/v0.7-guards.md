# montydyn v0.7 — Guards Convergence

**Date:** 2026-06-01
**Target:** `https://montydyn-v07.umg-bhalla88.workers.dev` (branch `feat/v0.7`, deployed `ddee32e0`, gzip 939.57 KiB / 10MB cap, `/health=ok`)
**R2 prefix:** `v07/` (isolated from `v05/`)

## The three guards (live + effective)

| Guard | Constant(s) | Purpose | Verdict |
|---|---|---|---|
| **G1 source cap** | `MAX_STDLIB_SOURCE_BYTES=512000` (500KB); `MAX_INLINE_STDLIB_BYTES=2097152` (2MB) | Typed `SizeAdmissionError` before any eval-into-heap on total injected stdlib source; fence on inline `config.stdlib` blob | **LIVE + EFFECTIVE** |
| **G2 mathjs opt-in** | `OPT_IN`/`stdlib-meta.optIn=[mathjs]` | `modules:true` excludes high-amplification libs; loads only when explicitly named, still counts vs the 500KB cap | **LIVE + EFFECTIVE** |
| **G3 dump ceiling (KEY FIX)** | `MAX_DUMP_BUFFER_BYTES` 45MB→18874368 (18MB); `MAX_RESTORE_RAW_BYTES` 45→18MB; `SCRUB_MAX_BUFFER_BYTES` 32→16MB (lockstep) | Clean-reject any buffer approaching the real ~24-30MB OOM cliff before the serialize transient can OOM | **LIVE + EFFECTIVE** |

### Is the v0.6 silent ~24-30MB OOM hole CLOSED?

**YES — closed for the dump path.** A cell allocating ~22MB incompressible bytes grows the WASM linear buffer to 24,379,392 bytes (~24.4MB) — squarely in the old 24-30MB silent-OOM band that lived *below* the prior 45MB ceiling. The per-cell checkpoint now clean-rejects:

```
checkpoint.error = 'SizeAdmissionError: linear-memory buffer 24379392 > MAX_DUMP_BUFFER_BYTES 18874368; refusing snapshot'
```

Socket stays **ALIVE** (alloc-eval returned, follow-up `{t:gen}` returned `ok:true`, no WS 1006), VM still usable in-memory (`1+1===2`). Confirmed by the adversarial pass independently (22MB incompressible alloc → typed reject, socket alive, VM usable).

**Lockstep invariant held:** restore and scrub ceilings lowered with the dump ceiling so "if it could be dumped it can be restored" stays true and scrub stays below the dump ceiling. When `createFresh` throws the cap error, `lib.rs` never stores the glue (assignment runs only after `await` succeeds) → no partial VM, socket alive via the normal error-reply path.

## Verdict: PASS on all 3 guards.

- **G1 source cap:** `{modules:[mathjs]}` (631999 B) → `SizeAdmissionError: selected stdlib source 631999 > MAX_STDLIB_SOURCE_BYTES 512000`, socket alive, recovers with safe create. `[lodash,zod,mathjs]` (788893 B) → same clean typed reject.
- **G1 inline fence:** 2,099,226-byte inline blob → `SizeAdmissionError: inline config.stdlib blob 2099226 > MAX_INLINE_STDLIB_BYTES 2097152`, socket alive; under-cap blob evals fine.
- **G2:** `modules:true` → `typeof math==='undefined'`, `optIn=[mathjs]`; mathjs named alone (617KB iife) trips the source cap.
- **G3:** as above — the dangerous uncatchable death is now a clean typed rejection.

## Gotchas / notes

- v0.6 source ships only 5 default libs (lodash/dayjs/nanoid/uuid/zod ~172KB), not the 14-lib battery the v0.6 doc discusses. v0.7 adds mathjs as the single opt-in lib; at 617KB it doubly demonstrates both G1 and G2.
- Removed stale 1.3MB `src/stdlib-bundle.txt` (hyphen) artifact; real build emits gitignored `src/stdlib.bundle.txt` (dot).
- Runners left in worktree: `v0.7/smoke-v07-guards.mjs`, `v0.7/test-all-v07.mjs`, `v0.7/regress-v07.mjs`, `v0.7/regress-cold.mjs`.

---

## Test-All

**Window verdict: PASS** on stdlib matrix, full regression, and adversarial containment. **Zero DO-kill, zero sandbox escape, zero WS-1006** across all three suites. AE dataset `montydyn_kernel` corroborates server-side (all caps/fences/dumps logged as typed `SizeAdmissionError`, never crashes).

### (1) Guards live + OOM hole

Confirmed above: all 3 guards live + effective; the v0.6 silent ~24-30MB OOM dump hole is **CLOSED** (clean typed reject at 18MB ceiling, socket alive, VM usable).

### (2) Test-all verdict

**Smoke (guards):** 13/13 PASS (`smoke-v07-guards.mjs`).

**STDLIB matrix:** PASS. 11-item checklist — 9 functional items all GREEN (defaults inject+run, subset selection, real-evict hibernation persistence gen 1→2 via `sqlite-restore` with NO re-inject, crypto-shim determinism byte-identical incl. uuid.v4, determinism identical across restore, SQLite store / 0 spurious R2, v0.5 no-modules regression, introspection catalog 6 modules). Items 5/8/9 = guard/cost/bundle, covered by guards + AE timing; bundle cap confirmed non-binding (v0.7 default bundle smaller than v0.6). All 4 new-guard sub-checks PASS.
- *One "FAIL" line in the automated runner is a harness sizing bug* (its 2,050,021-byte inline blob was correctly accepted, being under the 2,097,152 fence) — not a product defect.

**Full regression (v0.1–v0.6):** PASS — **zero regression**. All 9 checks GREEN: typed-error recovery, dynamic config+host tools survive evict, seeded determinism byte-identical, output capture + value preview, kv-state across cold restore, host.fetch allowlist (block/allow/off-list), loop preemption all 4 shapes socket-alive, eval→evict→cold-restore core state survives, cold-wake **p50 692ms** (613–783ms) / warm p50 177ms — sub-second, platform-bound, matches v0.4/v0.6.
- Initial "FAIL" lines were harness assertion artifacts, all re-verified GREEN.

**Adversarial fuzz (21 attacks + 5-point deep re-probe):** **NO escape, NO WS-1006, NO permanent brick.** Contained (typed error, socket alive, VM usable): stack-bomb recursion + apply(100k), 22MB incompressible alloc (the v0.6 hole, closed), string-doubling bomb, all 3 infinite-loop shapes (TimeoutError ~0.2-1s), ReDoS, deep-nested stringify, weird unicode/surrogates, throw-in-async + awaited reject, lodash `_.range(6M)`, lodash `_.cloneDeep` 100k-deep (RuntimeError, socket alive, recovers), escape probes (`process`/`require`/`__wbindgen_throw` undefined, `new Function('return this')` returns VM global only — no host reach), SSRF→`FetchBlockedError`, mem-bomb+reset recovers, source cap / mathjs opt-in, inline fence in-band (1.9MB ok / 2.0-2.05MB typed reject).

### (3) Bugs / risks (ranked)

| # | Severity | Finding |
|---|---|---|
| 1 | **Medium (containment GAP, not escape)** | **In-cell raw-allocation bombs lose the in-flight reply.** Three shapes: (a) unbounded `a.push(new Array(1000))` loop, (b) zod schema bomb `for 50000: s=z.array(s)` (258MB monotonic alloc — the documented v0.5 ~256MB edge; the 18MB dump guard fires but cannot shrink an already-allocated buffer), (c) inline `config.stdlib` ≥~2.1MB (oversized WS create frame wedges handling *before* the fence runs). All three: eval (and follow-up gen) **time out / hang, no typed error** — but TCP socket never emits 1006 and **reconnect always reconstructs a clean kernel** (committedCell=-1, no poisoned checkpoint). Degraded request-loss, NOT killed-session, NOT escape. Root cause: tick-budget + dump guards run at interrupt/checkpoint cadence; a single synchronous statement crosses the buffer ceiling *between* interrupt callbacks. |
| 2 | **Low** | Inline fence is bypassed by transport above ~2.05MB: CF edge drops ≥~2.1MB inbound WS frames before the DO sees them (still non-crashing, socket alive). The fence only emits its typed rejection up to ~2.05MB. |
| 3 | **Low (pre-existing, NOT a v0.7 regression — identical on v0.6)** | Top-level returned Promise shows value preview `{}` (the promise object), not resolved value. Fetch side-effects themselves correct. |
| 4 | **Low (pre-existing, out of regression scope)** | `capture:false` still buffered logs in a quick check. |

**Recommended follow-up (not release-blockers):** add an allocation-rate / used-heap tripwire inside the interrupt handler (reject when `getMemoryUsage().memoryUsedSize` crosses ~15MB mid-cell) to convert the 3 in-cell raw-alloc bombs into typed `SizeAdmissionError` like the dump path; reject oversized create frames by byte length before parse.

### (4) Merge recommendation: feat/v0.7 → main

**GO / YES.** All three merge gates met:
1. OOM hole closed — the v0.6 silent ~24-30MB dump OOM is now a clean typed reject, socket alive. ✓
2. No regression — all 9 v0.1–v0.6 regression checks PASS, cold-wake sub-second. ✓
3. No escape, no DO-kill, no WS-1006, no brick anywhere in 21 adversarial attacks + deep probe. ✓

The remaining containment gaps (risk #1/#2) are **request-loss / hang, recoverable on reconnect** — degraded, not session-killing or escaping. They are not release-blockers for the stated v0.7 goal but should be tracked as the next hardening item.

### (5) Next: Tier-0 extensions wire-up + V1 facets

- **Tier-0 extensions wire-up:** harden the in-cell allocation surface first (the mid-cell used-heap tripwire from risk #1) so the "every case returns a typed error, socket alive" guarantee becomes total — this is the natural foundation before exposing more extension surface. Add byte-length rejection of oversized create frames (risk #2) at the same time.
- **V1 facets:** with the dump/restore size envelope now pinned at 18MB and the OOM cliff converted to a typed reject, the facet model can rely on a stable, bounded snapshot size. Proceed with facet wire-up referencing `v1-facet-spike.md` / `v1-direction.md`.
